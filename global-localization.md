# 한국어·영어와 개인 알림 시간대

## 지원 범위와 날짜 정책

- 앱·서버 지원 언어는 `ko`, `en`이다. 기존 회원과 설정 미전송 앱의 기본값은 `ko`, `Asia/Seoul`이다.
- 개인 루틴·투두 예약 알림과 저녁 미완료 알림은 회원의 IANA 시간대를 사용한다. 고양이 복귀 알림의 09:00–21:00 발송 허용 시간도 회원 시간대다.
- **공동 미션·출석·재화 보상의 날짜 기준은 계속 KST**다. 루틴 기록·통계·주간 회고 집계와 회고 배치 시각도 이번 변경에서 현지 날짜로 바꾸지 않는다. 알림 시간대와 보상/통계 날짜를 같은 정책으로 해석하지 않는다.
- 시각 값은 기존 Instant/ISO-8601 계약을 유지한다. `timeZone` 변경으로 과거 기록의 날짜나 알림을 다시 쓰지 않는다.

## 계정 설정과 프론트 연결

`GET /api/v1/me`와 기존 `PUT /api/v1/me` 응답에 `language`, `timeZone`을 추가한다.

`PATCH /api/v1/me`는 인증된 본인의 언어·시간대만 부분 수정한다. 닉네임은 필요 없다.

```json
{"language":"en","timeZone":"America/New_York"}
```

- 두 필드는 각각 선택이며 적어도 하나가 필요하다. 생략/null은 기존 값 유지다.
- `language`: 정확히 `ko` 또는 `en`. `timeZone`: Java IANA TZDB에 등록된 ID(`Asia/Seoul`, `America/New_York`, `UTC` 등), 최대 64자. 고정 오프셋 `+09:00`, 임의 ID, 빈 요청은 400 `VALIDATION_FAILED`.
- 성공 응답은 `GET /me`와 같다. 인증·탈퇴 계정 차단은 기존 회원 API와 같다.
- 프론트는 로그인 후 기기에 선택된 앱 언어와 OS IANA 시간대를 동기화한다. 언어 변경과 앱 foreground에서 시간대 변경을 감지할 때 다시 전송한다. 여러 기기는 마지막 저장 설정을 공유한다.
- 매 API 요청에 앱 언어를 `Accept-Language: en` 또는 `ko`로 보낸다. 서버는 지원 언어 중 헤더 우선순위를 협상한다(`en-US` → `en`, `ko-KR` → `ko`). 헤더 미전송/지원 언어 없음은 `ko`. 응답은 `Vary: Accept-Language`를 포함한다.
- 헤더는 해당 응답의 표시 언어이며 계정 설정을 자동 수정하지 않는다. 비동기 푸시·AI는 저장된 `users.language`를 사용한다.

## 카탈로그 표시 이름

- `items`, `themes`, `characters`, `goals`, `gacha`에 nullable JSON `name_translations`를 추가한다. 예: `{"en":"Wooden chair"}`.
- 기본 `name`은 유지한다. 영어 요청이면 `name_translations.en`, 없거나 비어 있으면 기본 `name`을 사용한다.
- 상점/보유 아이템, 캐릭터/착용 장식, 방 렌더, 온보딩 목표, 집 목표, 뽑기 목록·보상에 동일 규칙을 적용한다.
- ID, `code`, 가격, 확률, `assetKey`와 이미지 파일은 변경하지 않는다. 사용자 닉네임·집 이름·채팅·루틴 제목 등 사용자 입력은 자동 번역하지 않는다.
- 기본 캐릭터 9종과 기존 목표·카테고리 뽑기의 알려진 코드는 프론트 영어 표기로 초기화한다. 운영자가 추가한 테마·아이템 이름은 번역을 별도로 등록해야 하며, 이 서버 구현만으로 모든 운영 카탈로그의 영문 검수가 끝난 것은 아니다.

관리자 세션·CSRF 계약을 따르는 번역 API:

- `GET /admin/catalog/{kind}/{id}/translations`
- `PUT /admin/catalog/{kind}/{id}/translations`, body: `{"nameTranslations":{"en":"Wooden chair"}}`
- `kind`: `items`, `themes`, `characters`, `goals`, `gacha`. 응답: `{name, nameTranslations}`.
- PUT은 번역 map 전체 교체다. 현재 키는 `en`만 허용하고 값은 1–120자(공백만 불가). `{ "nameTranslations": {} }`로 번역을 제거한다. 기본 이름·asset key·노출 상태는 보존한다. 없는 ID는 404 `CATALOG_TARGET_NOT_FOUND`.

## 서버 생성 문구

- 집 입퇴거·입주 신청·미션 달성·응원·거미줄, 루틴/투두 리마인드, 저녁 미완료, 고양이 복귀, 주간 회고 알림을 **수신자 언어**로 저장·발송한다. 버그 제보 답변 알림은 제목만 번역하고 운영자 작성 본문은 유지한다.
- 알림 내역은 발송 당시 언어 스냅숏이다. 설정 변경으로 과거 내역을 재번역하지 않는다. 같은 이벤트를 받는 집 멤버들의 언어는 독립적으로 처리한다.
- 입주 신청의 1시간 중복 억제는 한국어·영어 본문을 모두 확인하므로 언어 변경으로 우회하지 못한다. 기존 트랜잭션 안에 내역 저장, 커밋 후 push 계약은 유지한다.
- AI 주간 회고는 생성 시 저장된 언어로 작성한다. 영어 시스템 프롬프트에도 JSON 스키마·길이 제한·사용자 입력을 지시로 실행하지 않는 규칙을 적용한다. AI 실패 시 통계 기반 요약도 같은 언어다. 과거 회고를 다시 생성하지 않는다.
- 조정 추천은 기존 `message`를 유지하고 `messageCode`, `messageParams`를 추가한다. `message`는 요청 언어로 렌더링한 폴백이다. 과거 추천의 새 필드는 null이며 원문을 유지한다.

| messageCode | messageParams | 의미 |
| --- | --- | --- |
| `ADJUST_DROP_DAY` | `title`, `day` (`MON`–`SUN`), `weeks:3` | 3주 연속 실패한 요일 제외 제안 |
| `ADJUST_REDUCE_DAYS` | `title`, `days` (요일 코드 배열), `from`, `to`, `weeks:2`, `threshold:40` | 최근 2주 완료율에 따른 주간 빈도 축소 |

재화 이력의 기존 `reason`과 오류의 기존 `code`는 이미 언어 중립 식별자다. 프론트가 해당 코드로 번역한다. 오류 `message`는 기존 한국어 디버깅/폴백 문자열을 유지한다.

## 알림 시간대 세부 정책

- 예약 알림 배치는 한 번의 실행 기준 Instant를 실제 회원 시간대별 현지 날짜·시각으로 변환한다. 같은 시간대 회원은 묶어서 커서 조회한다. 개인별 전체 스캔/N+1 시간대 조회를 추가하지 않는다.
- 예약 시각과 루틴 반복 요일·시작/종료 날짜, 투두 기한 날짜는 그 알림의 현지 날짜로 대조한다. 예약 알림은 기존 5분 주기를 유지한다.
- 당일 중복 방지 구간은 현지 자정부터 다음 현지 자정까지의 Instant다. DST 시작/종료일의 23/25시간을 반영한다. 가을에 같은 시각이 두 번 오는 경우 같은 날짜 알림을 다시 적재하지 않는다. 봄에 사라지는 예약 시각은 그날 건너뛴다(지연 보상 발송 없음).
- 저녁 미완료 알림은 현지 21시 이후의 시간대별 날짜 job으로 적재한다. 같은 사용자·현지 날짜의 기존 unique 중복 방지를 유지하고, 실제 발송 전에 현지 날짜와 21시 이후인지 다시 확인한다. 현재 매시 트리거여서 30/45분 시차 지역은 21시 이후 첫 트리거에 발송한다.
- 고양이 복귀 알림은 30분마다 후보를 확인하고 수신자 현지 09:00 이상 21:00 미만에만 보낸다. 미접속 경과시간·상태 판단은 기존 정책을 유지한다.
- 여행/설정 변경은 다음 판정부터 적용한다. 지난 알림을 소급 발송하지 않는다. 기기 시간대 변경을 서버가 자동 추정하지 않으므로 프론트 동기화가 필요하다.
